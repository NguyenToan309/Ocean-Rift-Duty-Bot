# =====================================================================
# DUTY LOGGER — Biến môi trường mẫu
# Copy file này thành .env rồi điền giá trị thực
# TUYỆT ĐỐI KHÔNG commit file .env lên git
# =====================================================================

# ----- Discord Bot -----
DISCORD_BOT_TOKEN=MTQ5ODA0OTQxNTM1NTgzMDQzNw.GxKckO.ZqP3i3ULGuCjUzJO9PrsXSzH1SRSpfUNPZDCw4
DISCORD_CLIENT_ID=1498049415355830437
DISCORD_CLIENT_SECRET=p9ZItDpK61asp1bGz5A-GQaS3D_oocYH
# URL callback sau khi đăng nhập OAuth2 (điền domain VPS của bạn)
DISCORD_REDIRECT_URI=http://localhost:8000/auth/callback

# ----- PostgreSQL -----
DB_HOST=localhost
DB_PORT=5432
DB_NAME=duty_logger
DB_USER=duty_user
DB_PASSWORD=YoY9MJ4Dm_M7g3r9RVxaMwRjcFx3D57s
# SQLAlchemy async URL (tự build từ các biến trên — không cần sửa)
# DATABASE_URL=postgresql+asyncpg://duty_user:password@localhost:5432/duty_logger

# ----- Redis -----
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=cvR5_LBTjPa1rebPl7xJDjFSG2ZgYN6I
REDIS_DB=0

# ----- JWT / Auth -----
# Tạo bằng: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=cb3951be8ff98d73a656d4c22eca3aaa8a7e2bc7b089d8b4f0b9edfabed9c52e
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# ----- Mã hoá trường nhạy cảm (Fernet) -----
# Tạo bằng: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=vZI5cg54CyHPHWeSbGdQJ4HvcW-EBXgME_YokGz8Zv8=
# ----- HMAC ký file export -----
# Tạo bằng: python -c "import secrets; print(secrets.token_hex(32))"
HMAC_SECRET=1b1102676e18ebff88924aa2f0016a989cddabb09a272b09c8e9603fb131f6a7

# ----- Web Dashboard -----
WEB_HOST=0.0.0.0
WEB_PORT=8000
# Chỉ cho phép domain nội bộ/VPS truy cập (CORS)
ALLOWED_ORIGINS=http://localhost:8000
# Đặt False khi production
DEBUG=True

# ----- Export file -----
# Thư mục lưu file export tạm (nằm ngoài webroot)
EXPORT_DIR=./exports
# File export tự xóa sau bao nhiêu phút
EXPORT_TTL_MINUTES=10

# ----- Timezone mặc định -----
DEFAULT_TIMEZONE=Asia/Ho_Chi_Minh

# ----- Rate limit -----
RATE_LIMIT_DEFAULT=30/minute
RATE_LIMIT_EXPORT=2/5minutes
RATE_LIMIT_LOGIN=5/minute
